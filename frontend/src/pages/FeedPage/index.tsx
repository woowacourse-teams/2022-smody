import { FlexBox, Feed } from 'components';

const FeedPage = () => {
  return (
    <FlexBox flexDirection="column" alignItems="center">
      <Feed />
    </FlexBox>
  );
};

export default FeedPage;
