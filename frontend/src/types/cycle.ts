export type Cycle = {
  cycleId: number;
  challengeId: number;
  progressCount: number;
  startTime: string;
};

export type CycleDetail = {
  cycleDetailId: number;
  progressTime: string;
  description: string;
  progressImage: string;
};
